requests>=2.31.0
numpy>=1.24.0
pytz>=2024.1
python-dotenv>=1.0.0
anthropic>=0.40.0
schedule>=1.2.0

# Neon Postgres writer (agents/etl/partner_revenue_etl.py)
# Lands LL daily revenue into pgam_direct.ll_daily_partner_revenue for the
# admin.pgammedia.com Partner Revenue Dashboard.
psycopg[binary]>=3.1.0

# Daily report sync (reports/daily_report_sync.py)
google-auth>=2.28.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.120.0
gspread>=6.0.0

# Floor elasticity model (intelligence/floor_model.py)
pandas>=2.0.0
scikit-learn>=1.3.0
lightgbm>=4.0.0
joblib>=1.3.0

# MSN Partner Hub insights puller (agents/etl/msn_insights_etl.py).
# Playwright + Chromium drives an authenticated browser session against
# https://www.msn.com/en-us/partnerhub/... and calls api.msn.com from
# inside the page so MSAL auto-attaches the Bearer JWE. Requires a
# one-time `playwright install chromium` after pip-install.
# Not included by default in slim deploys (Render's free Python tier
# can't host Chromium); scheduler.py and the agent module both no-op
# gracefully when the import fails. Production cron lives in GitHub
# Actions (.github/workflows/msn-insights.yml).
playwright>=1.40.0
